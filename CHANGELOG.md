# Changelog

## 3.0.0 Beta 12

- Fixed `send_file()` placement inside `CP3Connection`.
- Restored `request()`, `send_png()`, and `_send_once()` as class methods.
- Experimental `media-upload` can now call the generic transfer path.
